// Creamos la conexion a la base de datos.
// Importamos o mandamos a llamar el archivo db.js que esta en al carpeta config.
// creamos las querys para obtener los datos de los usuarios.

const obtenerUsuario = () => {
  const query = `SELECT * FROM usuarios`;
};
